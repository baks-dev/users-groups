{% trans_default_domain 'groups.users' %}

{#{{ form_row(form.user, {
    label: 'Пользователь'|trans,
    placeholder: 'Выберите пользователя из списка'|trans,
    disabled: true,
    attr: { 'data-select' : 'select2' } })
}}#}

{{ form_row(form.group, {
    label: 'Группа'|trans,
    placeholder: 'Выберите группу из списка'|trans,
    attr: { 'data-select' : 'select2' }
}) }}